// 1. Write a Javascript code that takes astring as input and uses string methods to countthe number of characters in the string, excluding spaces. Then, display the character count.
// For example, if the input is "Hello, world!" , the code should display "12" (since there are 12 non-space characters in the string)


// METHOD 1
let word = "how are you doing today?";


// METHOD 2
let text = "Hell no";
console.log(text);
let textBeforeLength = text.length;
console.log(textBeforeLength);

let replacedText = text.replaceAll("", "");
console.log(replacedText);

let replacedTextLength = replacedText.length;
console.log(replacedTextLength);


// Write aJavascript code that takes a string andreplaces all occurences of a specified substring with another specified string, using string methods. Then, display the modified string.
// For example, if the input string is "Hello, world! Hello, u"



// ARRAY METHODS
let name = ["Man", 23, true, [2, false]];
let arr = Array("Nile", 3, true, ["Gold", null]);

console.log(name);
console.log(arr);

console.log(name [3][0]);
console.log(name[3][2][1]);















